import { style } from "@vanilla-extract/css";

export const loginAllContainer = style({
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
  width: "100vw",
  height: "100vh",
});

export const prevButton = style({
  fontSize: "1.4rem",
  marginBottom: "0.5rem",
});

export const stepBoxContainer = style({
  width: "45rem",
  display: "flex",
  flexDirection: "column",
});

export const title = style({
  fontSize: "2rem",
  fontWeight: "800",
  marginBottom: "1.8rem",
});
